spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://${db.ip}/personal_assistant?characterEncoding=utf-8&useSSL=false
    username: ${db.name}
    password: ${db.password}


